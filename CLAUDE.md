# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

See **[AGENTS.md](./AGENTS.md)** for the full guidance: build/run commands, the
three-stage Lean pipeline (`Animate.lean`), the supporting modules
(`Annotations`, `StringMatching`, `HighlightSyntax`), and the Blender renderer.
